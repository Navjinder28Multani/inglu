{
  "name": "contact-manager-backend",
  "version": "1.0.0",
  "description": "Backend for contact management app",
  "main": "server-test.js",
  "scripts": {
    "start": "node server-test.js",
    "dev": "nodemon server-test.js",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
